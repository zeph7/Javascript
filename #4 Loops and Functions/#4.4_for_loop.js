// JS Code to explain --- for Loop ---


const days = ['Mon', 'Tue', 'Wed', 'Thr', 'Fri', 'Sat']

// for loop
for(let index = 0; index < days.length; index++){
	console.log(`Day ${index + 1} -- ${days[index]}`)
}

	// How does a for Loop works ?

// for( initialization ; condition ; updation ) {-- Block of Code --}